import { environment } from '../../../../environments/environment';

export const endpoint = {

    
    Category: `${environment.serverUrl}/category`,
   
    Product: `${environment.serverUrl}/product`,

    Supplier: `${environment.serverUrl}/supplier`
    
  }